import { storiesOf } from '@storybook/react-native';
import React from 'react';
import StackBackHeader from '.';

storiesOf('Navigation.StackBackHeader', module)
  .add('StackBackHeader', () => <StackBackHeader />);
